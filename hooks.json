{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "SCRIPT=\"${GEMINI_PLUGIN_ROOT}/.agents/hooks/session-start-gemini.sh\"; [ -f \"$SCRIPT\" ] || SCRIPT=\"${GEMINI_PROJECT_DIR}/.agents/hooks/session-start-gemini.sh\"; [ -f \"$SCRIPT\" ] && bash \"$SCRIPT\" || true"
      }
    ]
  }
}
