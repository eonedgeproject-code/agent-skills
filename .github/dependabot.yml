version: 2
updates:
  # Keep GitHub Actions (checkout, setup-bun, …) current.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: chore
